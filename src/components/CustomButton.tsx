import React from 'react';
import {Pressable, Text} from 'react-native';

const CustomButton = () => {
    return (
        <Pressable>
            <Text>Button</Text>
        </Pressable>
    );
};

export default CustomButton;
